export interface ProductDTO {
  title: string;
  description: string;
  price: number;
}

export interface Product {
  id: string;
  title: string;
  description: string;
  price: number;
  count: number;
}
